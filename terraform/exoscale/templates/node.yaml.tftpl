#cloud-config

package_update: true
packages:
  - curl
  - git
  - tar
  - wireguard-tools

runcmd:
  # ── k3s agent install ─────────────────────────────────
  - echo "Installing dependencies..."

final_message: "node is ready, after $UPTIME seconds"
